{# src/AppBundle/Resources/views/Entity/NfcTag/CRUD/readItem.html.twig #}
{% extends 'AppBundle:Entity/NfcTag/Base:item.html.twig' %}

{% block active_nfc_tag_update %}{{ 'class="active"' }}{% endblock %}

{% block data %}
    <div id="read-data-holder">

        <div class="separator-holder">
            <h2>Параметры</h2>
        </div>
        
        <ul>
            <li>
                <div class="item key">
                    <span>{{ 'nfc_tag.number.label'|trans({}, 'forms') }}</span>
                </div>
                <div class="item value">
                    <p>{{ nfcTag.number }}</p>
                </div>
            </li>
            <li>
                <div class="item key">
                    <span>{{ 'nfc_tag.code.label'|trans({}, 'forms') }}</span>
                </div>
                <div class="item value">
                    <p>{{ nfcTag.code }}</p>
                </div>
            </li>
        </ul>
    </div>

    {% if nfcTag.operator|length %}
        <div class="separator-holder">
            <h2>Оператор, которому принадлежит NFC-метка</h2>
        </div>

        {% include 'AppBundle:Entity/Operator/Binding:ancestors.html.twig' with { 'sibling': nfcTag } only %}
    {% endif %}

{% endblock %}
