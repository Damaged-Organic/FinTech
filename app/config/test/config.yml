# app/config/test/config.yml
imports:
    - { resource: ../test/config.yml }

# Framework
framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

# Profiler
web_profiler:
    toolbar: false
    intercept_redirects: false

# Swiftmailer
swiftmailer:
    disable_delivery: true
